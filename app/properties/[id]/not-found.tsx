import { NotFound } from "@/components/ui/not-found"

export default function PropertyNotFound() {
	return (
		<NotFound 
			title="Property Not Found"
			description="Sorry, we couldn't find the property you're looking for."
		/>
	)
}
